name: meta_orchestrator
display_name: 议事编排官 (Deliberation Orchestrator)
version: "1.0.0"
description: |
  AICouncil的智能规划器，负责分析用户需求并设计最优讨论方案。
  核心能力：
  - 理解需求，识别所需专业能力维度
  - 从现有角色中匹配最合适的角色
  - 如无匹配角色，调用角色设计师生成新角色
  - 选择最适合的讨论框架（罗伯特议事规则、图尔敏论证法等）
  - 输出完整的可执行配置方案

stages:
  planning:
    prompt_file: meta_orchestrator_prompt.txt
    schema: OrchestrationPlan
    input_vars:
      - user_requirement
      - available_roles
      - available_frameworks

default_model: deepseek-reasoner

parameters:
  max_retries: 3
  temperature: 0.8
  max_tokens: 6000
  tools_mode: "auto"  # auto调用工具，无需用户确认

# 工具调用配置
tools:
  - name: list_roles
    description: "获取系统中所有可用角色的列表（包括用户自定义角色）"
    enabled: true
    
  - name: create_role
    description: "调用角色设计师生成新角色"
    enabled: true
    max_concurrent: 3  # 最多同时生成3个角色
    
  - name: select_framework
    description: "从框架库中选择最适合的讨论框架"
    enabled: true

tags:
  - meta
  - orchestration
  - planning
  - core

ui:
  icon: "🧭"
  color: "#7C3AED"
  description_short: "智能分析需求，规划讨论方案"
  description_full: |
    议事编排官是AICouncil的智能规划层，在讨论开始前分析用户需求，
    自动匹配或生成合适的角色，选择最优的讨论框架，
    为复杂问题设计结构化的解决方案。

capabilities:
  - 需求分析：理解复杂问题的多维度需求
  - 角色匹配：从现有角色库中找到最合适的专家
  - 角色生成：调用AI生成缺失的专业角色
  - 框架选择：基于问题类型推荐最优讨论框架
  - 配置优化：自动确定Agent数量、讨论轮次等参数
