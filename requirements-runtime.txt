# AICouncil 运行时依赖（用于EXE打包）
# 相比 requirements.txt 已移除开发/测试工具

# ═══════════════════════════════════════════════════════════
# 核心运行时依赖 - 必须打包
# ═══════════════════════════════════════════════════════════
langchain>=0.1.0
langchain-core>=0.1.0
pydantic>=2.0.0
flask>=3.0.0
requests>=2.28.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0

# ═══════════════════════════════════════════════════════════
# 可选依赖 - 根据打包方案选择
# ═══════════════════════════════════════════════════════════

# 方案A（轻量级 ~100MB）- 注释掉以下两行
# playwright>=1.40.0      # 延迟安装：首次PDF导出时提示
# DrissionPage>=4.0.0     # 简化搜索：仅用Yahoo/Mojeek

# 方案B（完整功能 ~250MB）- 取消以下两行注释
playwright>=1.40.0        # 内嵌Chromium：离线PDF导出
DrissionPage>=4.0.0       # 浏览器搜索：支持Baidu/Bing

# ═══════════════════════════════════════════════════════════
# 已移除的开发依赖（不打包进EXE）
# ═══════════════════════════════════════════════════════════
# pytest>=7.0.0           # 单元测试工具
