# AICouncil 核心依赖（最小化安装）
# 用于 PyInstaller 打包时减小 EXE 体积

# 核心框架
langchain>=0.1.0
langchain-core>=0.1.0
pydantic>=2.0.0

# Web 服务
flask>=3.0.0

# HTTP 请求和解析
requests>=2.28.0
beautifulsoup4>=4.12.0

# 环境变量
python-dotenv>=1.0.0

# 注意：以下依赖为可选，请根据需要安装
# - playwright>=1.40.0  （高质量PDF导出，推荐安装）
# - DrissionPage>=4.0.0 （Baidu/Bing搜索增强，可选）
# - pytest>=7.0.0       （开发测试用，打包不需要）
